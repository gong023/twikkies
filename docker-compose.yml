services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: twikkies
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "6555:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
